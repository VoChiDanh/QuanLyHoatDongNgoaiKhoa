@model IEnumerable<QL_HDNK.Models.TrangThaiDangKy>

@{
    ViewBag.Title = "Danh sách";
}

<h2>Danh sách</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTTDK)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTTDK)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaTTDK }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaTTDK }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaTTDK })
        </td>
    </tr>
}

</table>



