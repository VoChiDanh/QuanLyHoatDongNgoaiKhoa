@model IEnumerable<QL_HDNK.Models.MucDiemRenLuyen>

@{
    ViewBag.Title = "Danh sách";
}

<h2>Danh sách</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenMucDRL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Diem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DanhMucDiem.TenDanhMuc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenMucDRL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Diem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DanhMucDiem.TenDanhMuc)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaMucDRL }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MaMucDRL }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaMucDRL })
        </td>
    </tr>
}

</table>



